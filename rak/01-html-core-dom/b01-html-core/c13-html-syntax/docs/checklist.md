# Checklist C13 - The HTML Syntax

- README menjelaskan alur: bytes -> decode -> tokenizer -> tree builder -> DOM.
- Ada penjelasan singkat kenapa parsing HTML error-tolerant dan stateful.
- Ada integration points yang jelas: blocking script, perubahan mode, re-entrancy (konsep).
- Istilah kunci disebut dan dipakai konsisten.
- Contoh skenario ada dan sesuai dengan fokus runtime (bukan tutorial API).
- Sumber referensi mencantumkan versi/tanggal dan lokasi multipage.
- SVG dibuat terakhir dan diagramnya spesifik untuk parsing (bukan template bab lain).

