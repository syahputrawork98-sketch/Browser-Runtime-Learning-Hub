# Checklist C14 - The XML Syntax

- README menjelaskan perbedaan HTML parser vs XML parser (strict vs error-recovery).
- Ada alur pemilihan parser berbasis `Content-Type`.
- Ada penjelasan dampak fatal parse error terhadap hasil DOM dan UX.
- Istilah kunci (MIME type, well-formedness, namespace) dipakai konsisten.
- Ada contoh skenario yang memulai dari respons server.
- Sumber referensi mencantumkan versi/tanggal dan lokasi multipage.
- SVG dibuat terakhir dan diagramnya spesifik untuk "parser selection" (unik untuk bab ini).

