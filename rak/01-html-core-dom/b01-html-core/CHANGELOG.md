# Changelog

Changelog ini hanya untuk **buku ini** (`R01/B01`).

## Unreleased

Belum ada perubahan baru.

## Releases

### R01-B01-v1 | 2026-03-13

#### Added

- Struktur buku `R01-B01` dengan 17 chapter (`c01` sampai `c17`) mengikuti referensi induk (HTML Standard multipage).
- Struktur per chapter: `README.md` + `docs/` + `examples/` + `assets/`.
- SVG overview unik per chapter (dibuat setelah konten stabil), termasuk untuk C13-C17.
- Dokumen pendukung per chapter: `docs/reference-notes.md` dan `docs/checklist.md`.
- Skenario contoh per chapter (minimal 1 file) untuk membantu pembacaan runtime.

#### Changed

- Arsitektur bab dipindahkan agar folder chapter sejajar dengan `docs/` di level buku.
- Materi lama disimpan sebagai arsip (legacy) di `c01-introduction/docs/legacy-c01-html-core/`.
- Materi lama disimpan sebagai arsip (legacy) di `c13-html-syntax/docs/legacy-c09-html-syntax-parsing/`.
- Materi lama disimpan sebagai arsip (legacy) di `c15-rendering/docs/legacy-c10-rendering-html-defined/`.

#### Notes

- Baseline referensi yang dipakai saat penulisan awal: HTML Standard (WHATWG) tanggal 2026-03-12.
